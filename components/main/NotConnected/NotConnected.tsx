import React from "react";
import styled from "styled-components";

const ComponentContainer = styled.div`

`;

type TComponent = {

}

const NotConnected = ({}: TComponent) => {
  return (
    <ComponentContainer>
adsadsa
    </ComponentContainer>
  );
};

export default NotConnected;